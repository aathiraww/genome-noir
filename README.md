# GENOME NOIR

The Synthetic Pet Lab — design the genome, shape the species, raise the future.

Static site: `index.html`, `styles.css`, `app.js`, and `assets/`.

## Live site

After GitHub Pages is enabled, the site is served at:

**https://aathiraww.github.io/genome-noir/**

## Enable GitHub Pages

1. Open **Settings → Pages** on this repository.
2. Under **Build and deployment**, set **Source** to **GitHub Actions**.
3. Merge this branch (or push to `main`) so the **Deploy GitHub Pages** workflow runs.

If the repository is private, GitHub Pages requires a plan that includes Pages for private repos (or make the repo public).

## Local preview

```bash
python3 -m http.server 8080
```

Then open http://localhost:8080
